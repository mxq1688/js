import { createStore } from 'vuex'

const state = {
}
const mutations = {
}

const actions = {
}
export default createStore({
  state: state,
  mutations: mutations,
  actions: actions,
  modules: {},
  strict: process.env.NODE_ENV !== 'production'
})
