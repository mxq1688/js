{
  "name": "mylib",
  "version": "1.0.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "module": "lib/index.js",
  "scripts": {
    "build": "rimraf lib && node build.js && npm run ts-types",
    "ts-types": " tsc --emitDeclarationOnly --outDir lib"
  },
  "devDependencies": {
    "esbuild": "^0.19.9",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
